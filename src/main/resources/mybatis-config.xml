<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 1. MyBatis核心设置（必须在environments前面） -->
    <settings>
        <!-- 下划线转驼峰 -->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
        <!-- 打印SQL日志（开发环境） -->
        <setting name="logImpl" value="STDOUT_LOGGING"/>
    </settings>

    <!-- 2. 环境配置（必须在settings之后、mappers之前） -->
    <environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <!-- 注意：纯JavaSE环境不能用Spring的Hikari数据源，改用MyBatis默认的池化数据源工厂 -->
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.cj.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/report_tool?useUnicode=true&amp;characterEncoding=utf8&amp;useSSL=false&amp;serverTimezone=Asia/Shanghai&amp;allowPublicKeyRetrieval=true"/>
                <property name="username" value="root"/>
                <property name="password" value="Jasper_MySQL8"/>
                <!-- 连接池参数 -->
                <property name="poolMaximumActiveConnections" value="10"/>
                <property name="poolMaximumIdleConnections" value="5"/>
            </dataSource>
        </environment>
    </environments>

</configuration>